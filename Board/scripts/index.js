'use strict';

(function () {

    
    const {remote} = window.parent.require('electron');
    var ipcRenderer = window.parent.require('electron').ipcRenderer;

    document.addEventListener ("DOMContentLoaded", init, false);

    function init () {
        //window.location.replace("http://webenv.io");
        
    }
});